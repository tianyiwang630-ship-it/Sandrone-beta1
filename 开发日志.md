# 开发日志

## 2026-06-12

### 背景
`agent-alpha/路径管理说明.md` 对 skill 路径仍使用旧表述，把 `agent-alpha/skills` 写成主路径、`home/.agents/skills` 写成兼容路径；这与当前第三方 skill 推荐安装到 `agent-alpha/home/.agents/skills` 的实际规则不一致。

### 关键更改内容
- 更新 `agent-alpha/路径管理说明.md`，明确第三方 skill 的主要安装位置是 `agent-alpha/home/.agents/skills`。
- 明确 `agent-alpha/skills` 是内置 skill、旧兼容路径或同名覆盖路径；加载时两个目录都会扫描，且 `agent-alpha/skills` 在同名冲突时优先。

### 背景
`ljg-plain`、`ljg-paper` 等本地第三方写作 skill 沿用了上游 Denote 习惯，硬编码要求写入 `~/Documents/notes/`。在 alpha 中 `HOME` 会被重定向到 `agent-alpha/home`，导致用户请求生成文档时默认落到 `home/Documents/notes`，而不是当前 workspace。

### 关键更改内容
- 更新 `agent-alpha/home/.agents/skills/ljg-plain/SKILL.md` 和 `ljg-paper/SKILL.md`，删除固定输出目录规则，改为不指定输出目录，遵循当前 agent/runtime 默认输出位置或用户明确指定位置。
- 更新 `ljg-book` 和 `ljg-relationship`，把“写入 `~/Documents/notes/...`”改为只规定文件名和 org 格式。
- 删除 `ljg-paper` 中指向 `~/.claude/...` 的旧个人路径引用，避免误导 alpha 读取外部 agent 目录。

### 背景
`agent-alpha` 在同一会话中安装新 skill 后，`load_skill` 仍使用启动时扫描到的缓存，导致新装到 `home/.agents/skills` 的 skill 返回 Unknown。模型随后可能误判为路径问题，把同一个 skill 再复制到 `skills/`，造成双目录重复安装。

### 关键更改内容
- `agent-alpha/agent/core/skill_loader.py` 在 `get_content()` 找不到目标 skill 时，会先重新扫描一次 skill 目录，再决定是否返回 Unknown。
- 新增 `agent-alpha/tests/core/test_skill_loader.py` 回归测试，覆盖“loader 启动后才安装新 skill，当前会话仍可 load”的场景。

## 2026-06-09

### 背景
`agent-alpha` 的有头浏览器异常退出、会话中断或 CLI 被关闭后，`state/browser/profiles/*/.headed.lock` 可能残留。旧锁会让无头浏览器误判长期 profile 仍被有头占用，导致无法复制 `default` 到 runtime 副本。

### 关键更改内容
- `agent-alpha/agent/tools/browser_manager.py` 在 `BrowserManager` 初始化时清理旧的 `runtime/`、`sessions/`、`sockets/` 和 `profiles/*/.headed.lock`，但保留 `downloads/` 和长期 `profiles/*/user-data`。
- 无头浏览器启动前不再被旧 `.headed.lock` 阻塞，而是先清理锁，再复制长期 profile 到本次 runtime 副本。
- 无头复制 profile 第一次失败时，会只清理命令行中包含当前项目 `state/browser` 路径的 alpha 浏览器进程，并重试一次；不会按 `chrome.exe` 名称批量清理，也不会影响用户日常 Chrome profile。
- 第二次复制仍失败时返回明确错误，提示关闭 alpha 打开的浏览器窗口后重试。
- 扩展 `agent-alpha/tests/core/test_browser_tools.py`，覆盖启动清理、旧锁清理、复制失败重试、进程路径边界匹配，以及普通 Chrome profile 不被清理。

## 2026-06-06

### 背景
当前 `agent-alpha` 的 `session-log/sessions/*.json` 只在本轮结束、恢复前保存或中断后才更新，`session-log/logs/*.json` 主要用于退出时归档。用户希望在消息发出后、工具调用仍在进行时，就能直接从文件看到 `history` 的新增项和工具返回，不再依赖双击 `Esc` 或等待最终回答。

### 关键更改内容
- 新增 `agent-alpha/agent/core/session_events.py`，提供实时事件 writer，以及工具结果截断逻辑。
- `agent-alpha/agent/core/agent_loop.py` 新增统一的 history 追加 helper；凡是新的 `user`、`assistant`、`tool` 项进入 `history`，都会同步追加到 `session-log/events/<session_id>.jsonl`。
- 实时事件日志采用薄包装结构：保留原始 `history` 项作为 `entry`，外层只补 `ts`、`seq`、`session_id`，避免额外发明复杂事件模型。
- 初版单行 `jsonl` 对人工阅读太差，后续改为“多行格式化 JSON 块 + 块间空行”的 append-only 格式；扩展名保持 `.jsonl`，但更接近可读日志，而不是严格 NDJSON。
- 工具结果新增统一截断规则：最多保留 `200` 行，并继续受既有字符上限控制；一旦截断，会在事件外层写入 `truncated`、`original_line_count`、`original_char_count`。
- `agent-alpha/agent/core/agent_runtime.py` 和 `agent-alpha/agent/cli/main.py` 接入 `events` 目录初始化与 runtime 传参，但不改变 `sessions/*.json` 和 `logs/*.json` 的职责。
- 新增 `agent-alpha/tests/core/test_session_events.py`，并扩展 `agent-alpha/tests/core/test_agent_loop.py`，覆盖实时写入链路、顺序号、多行块格式，以及工具结果超过 `200` 行时的截断标记。

### 背景
session `6bcc05` 中，模型为了运行 `agent-reach doctor` 手动写了 `cd D:\files\demo\0312-newagent\agent-alpha && ...`，但实际执行环境是 bash，Windows 反斜杠路径被 bash 误解析，导致命令失败。`BashTool` 本身已经默认在 `project_root` 执行，因此需要把默认工作目录暴露给模型，并提供更稳的临时目录参数。

### 关键更改内容
- `agent-alpha/agent/tools/bash_tool.py` 的工具说明明确告知模型：`command` 默认运行在当前 `project_root`，通常不要写 `cd ... && ...`。
- `bash` 工具新增可选 `working_dir` 参数；不传时继续使用 `project_root`，传相对路径时按 `project_root` 解析，传绝对路径时必须位于 `project_root` 内部。
- 新增简单 `cd 目录 && 命令` 防呆：自动拆成 `working_dir` 和实际命令，避免 Windows 路径直接交给 bash 解析；复杂 `cd` 用法会返回清晰提示，要求改用 `working_dir`。
- 补充 Git Bash 风格路径解析，在 Windows 上可把动态 `/d/...` 这类路径转成本机绝对路径；代码和测试均不硬编码本机项目路径。
- 新增 `agent-alpha/tests/core/test_bash_tool_working_dir.py`，覆盖默认目录、相对/绝对 `working_dir`、项目外拒绝、简单 `cd` 拆分、复杂 `cd` 指导和 Git Bash 路径解析。
- 调整 `agent-alpha/.gitignore`，不再忽略 `agent/tools/` 下的正式源码文件；继续只忽略 `agent-alpha/tools/` 这类本地运行时目录。

### 背景
`agent-alpha/session-log/events/ebfe4b.jsonl` 中，`twitter user-posts karpathy -n 3/5 --json` 在 BashTool 里看起来一直转，但直接用同一套运行时环境调用 `twitter` 可以返回。排查确认根因不是 `twitter.exe` 或 `agent-reach`，而是 BashTool 启动子进程后只轮询进程是否结束，没有持续读取 `stdout/stderr`；当子进程输出超过管道缓冲区时，子进程会被堵住，父进程也一直等不到退出。

### 关键更改内容
- `agent-alpha/agent/tools/bash_tool.py` 改为在子进程运行期间用后台线程持续读取 `stdout` 和 `stderr`，主线程继续负责 timeout 和 ESC 中断检测。
- 命令正常结束、timeout、ESC 中断时都会返回已经读取到的输出，并保留原有返回字段和 50,000 字符截断规则。
- 新增 `agent-alpha/tests/core/test_bash_output_streaming.py`，覆盖大 stdout、大 stderr，以及 timeout 后仍返回部分输出，防止类似管道堵塞问题回归。

## 2026-06-01

### 背景
最新 session 中，`open-websearch` 被模型传入不支持的 `google` 搜索源后报参数校验错误；随后用户 ESC 中断了包含多个工具调用的回合，历史里留下“assistant 声明了多个 tool_calls，但只记录了部分 tool 结果”的残缺消息，导致下一轮 OpenAI API 返回 400。

### 关键更改内容
- `agent-alpha/mcp-servers/open-websearch/mcp.config.json` 明确设置 `ALLOWED_SEARCH_ENGINES` 白名单，并在描述里列出允许的搜索源，说明 Google 不受支持，降低模型误选 `google` 的概率。
- `agent-alpha/agent/core/agent_loop.py` 在新用户输入前和发送给 LLM 前补齐缺失的 tool 结果；ESC 中断工具批次时，保留已经完成的工具结果，并为剩余未执行工具写入“用户中断，未执行”的 tool 消息。
- `agent-alpha/tests/core/test_agent_loop.py` 增加回归测试，覆盖旧 session 残缺 tool_calls 修复和工具批次中途 ESC 的补齐行为。

## 2026-05-31

### 背景
`agent-alpha/README.md` 之前仍然主要沿用 “Skills + MCP 框架” 的表述，和当前项目实际重心已经不一致。现在的 alpha 更需要被说明为一个 agent runtime：重点在环境管理、skill 安装与加载、浏览器自动化状态、沙盒边界，以及为后续多 agent / subagent / 持续运行模式做准备。

### 关键更改内容
- 重写 `agent-alpha/README.md` 的整体结构，改为“项目定位 -> 第一部分：现有设计 -> 第二部分：未来方向”的主线。
- 新增仓库根 `README.md`，内容与 `agent-alpha/README.md` 保持一致，避免根目录说明和 alpha 主文档出现两套叙事。
- 在现有设计中突出四个重点模块：环境管理层、skill 安装与加载机制、浏览器自动化设计、沙盒设计。
- 在 skill 章节明确说明双路径加载、`load_skill` 按需展开，以及为什么 alpha 可以承载复杂、非标准化 skill，而不只是简单 prompt 或标准工具函数。
- 根据实际实现补写 skill 安装链路，说明安装器会先读 skill 仓库安装文档、翻译路径和依赖命令，因此能顺带把 Python / npm / Go 依赖纳入 alpha 的运行时。
- 根据浏览器实现补写 profile 机制，明确默认主 profile 可由宿主 Chrome `Profile 5` 同步而来；有头模式直接使用并保留持久 profile，无头模式复制 profile 到临时 runtime 目录后执行并删除副本，CDP 模式则直接接管用户浏览器，不属于本地 profile 生命周期。
- 重排两份 README 的文档结构：增加中英双语切换入口、总目录、分节锚点，以及每个主要章节的“返回目录”跳转，降低长文档查找成本。
- 按新的阅读顺序调整 README 导航：中文放在前面，英文放在后面；中文区域和英文区域分别维护各自的目录与“返回目录”跳转，不再在文档开头混放一份双语总目录。
- 在未来方向中补充 agent 实例化、多 agent 平台准备、subagent 工具设计、持续运行模式、自动审查机制、自我模块与记忆等规划描述。

## 2026-05-29

### 背景
Agent Reach 的主 `SKILL.md` 仍然保留旧的 `bird` / `user-tweets` 示例，但本地参考文档和实际可用命令已经切换到 `twitter` / `user-posts`。这会让新会话里的 AI 在第一步就走错入口。

### 关键更改
- 更新 `agent-alpha/home/.agents/skills/agent-reach/SKILL.md` 的 Twitter/X 主说明：将执行入口从 `bird` 改为当前实际可用的 `twitter`。
- 在主说明中补充 alpha 环境约定：通过 PATH 直接调用 `twitter`，不要把 `bird` 或 `twitter-cli` 当成执行命令名。
- 调整 Troubleshooting 的 Twitter 提示，明确 `twitter user-posts` / `twitter feed` 是优先入口，`twitter search` 可能不稳定。

## 2026-05-29

### 背景
Agent Reach 已安装并配置成功后，新会话仍会优先按旧的 `bird user-tweets` 示例调用 Twitter/X；同时 `twitter-cli` 的可执行文件位于 `agent-alpha/bin`，但该目录没有进入 bash 的 `PATH`，导致 AI 即使用正确命令也可能找不到工具。
进一步排查后发现，路径事实分散在启动脚本、Python runtime、系统提示词和安装 skill 文档中，后续仍可能因为某一处漏改再次出现路径不一致。

### 关键更改
- `agent-alpha/agent/core/runtime_paths.py` 和 `agent-alpha/start-agent-alpha.ps1` 现在会创建 `agent-alpha/bin`，并把它放入 bash/启动环境的 `PATH`，让 `uv tool` 安装出来的 CLI shim 可直接调用。
- `agent-alpha/start-agent-alpha.ps1` 改为调用 `agent.core.runtime_paths.build_runtime_env()` 生成启动环境，不再手写 HOME、PATH、TEMP、APPDATA 等路径变量，避免启动脚本和 bash runtime 各维护一套路径事实。
- `agent-alpha/agent/core/runtime_paths.py` 增加 `AGENT_ALPHA_ROOT`，并禁止 `runtime_env.local.json` 覆盖 HOME、PATH、TEMP、APPDATA、AGENT_ALPHA_ROOT 等路径策略变量；该文件只用于 token/cookie/API key/proxy 等服务配置。
- `agent-alpha/agent/core/system_prompt_builder.py` 补充说明 `agent-alpha/bin` 也在 PATH 前部。
- `agent-alpha/skills/install-alpha-skill/SKILL.md` 同步补充 `AGENT_ALPHA_ROOT`、`agent-alpha/bin`、runtime env profile 的用途和禁止覆盖路径变量的规则。
- `agent-alpha/.gitignore` 忽略 `bin/`，避免本地工具 shim 进入版本管理。
- 更新 `agent-alpha/home/.agents/skills/agent-reach/SKILL.md` 的 Twitter/X 示例，从旧的 `bird` 改为当前实际可用的 `twitter user-posts` 等命令，并指向 `references/social.md` 查看细节。
- 补充 runtime path、启动脚本、系统提示词和安装 skill 测试，覆盖 `agent-alpha/bin` 的创建与 PATH 注入、启动脚本单一 env 来源、profile 不可覆盖核心路径变量。

## 2026-05-29

### 背景
Agent Reach 这类通过 CLI 工作的 skill 需要更宽松的 bash 运行边界：普通 CLI、带参数命令、平台工具调用应能直接运行，但仍要阻止 AI 直接写出 `agent-alpha` / workspace 之外，或修改系统设置。

### 关键更改
- `agent-alpha/agent/core/sandbox_guard.py` 调整 bash 默认策略：未知但不危险、且没有明确外部写入目标的 CLI 命令默认放行。
- `agent-alpha/agent/core/command_path_extractor.py` 增加对 PowerShell、cmd、Linux shell 常见写文件命令和危险系统命令的识别，覆盖 `Set-Content`、`cmd /c echo >`、`touch/rm/cp/mv`、`Set-ExecutionPolicy`、`reg`、`netsh`、`curl | bash` 等场景。
- `agent-alpha/agent/core/system_prompt_builder.py` 补充当前 bash 运行情况：cwd、PATH、runtime env profile、可写范围、外部路径限制，以及 README 中宿主路径应翻译到 alpha 内部目录。
- 扩展 bash sandbox 和系统提示词测试，验证普通 CLI 放行、外部写入拒绝、系统设置拒绝、npm/go 例外保持不变。

## 2026-05-29

### 背景
Agent Reach 这类 CLI 型 skill 在新会话里能加载 skill，但 bash 子进程拿不到上次配置的 token/cookie，导致 `twitter.exe` 等工具提示未认证。

### 关键更改
- `agent-alpha/agent/core/runtime_paths.py` 现在会读取 `agent-alpha/config/runtime_env.local.json`，并把其中的环境变量注入 bash 子进程运行环境。
- `agent-alpha/agent/core/system_prompt_builder.py` 增加提示：用户在配置 skill、CLI、MCP 或外部服务时提供 token/cookie/API key/auth header，默认保存到本地 runtime env profile，除非用户明确说临时使用或不要保存。
- `agent-alpha/.gitignore` 忽略 `config/runtime_env.local.json`，避免本地凭据配置进入版本管理。
- 补充 `runtime_paths` 和系统提示词测试，覆盖 env profile 注入和提示词规则。

## 2026-05-29

### 背景
需要收束 `agent-alpha` 的 skill 安装与运行路径边界：取消用户目录 `~/.agent-alpha` 方向，改为在项目内部模拟运行 home/config/cache/temp 等目录，同时允许 workspace 正常写入，并避免安装第三方 skill 时误写到 Claude/Codex/OpenClaw 的用户级 skill 路径。

### 更改内容
- 新增 `agent-alpha/agent/core/runtime_paths.py`，统一创建 `home`、`userfile`、`cache`、`tools`、`data`、`state` 等项目内运行目录，并生成指向这些目录的 runtime env。
- 调整 `start-agent-alpha.ps1` 和 `BashTool`，启动和执行 shell 命令时注入项目内 `HOME`、`USERPROFILE`、`XDG_*`、`APPDATA`、`TEMP`、`PIP_CACHE_DIR`、`UV_CACHE_DIR`、`HF_HOME`、`PLAYWRIGHT_BROWSERS_PATH` 等变量，并把 `.venv/Scripts` 放到 `PATH` 前面。
- 调整 sandbox 路径策略：外部路径允许读取但禁止普通写入；workspace 与 alpha 非核心运行目录可写；`agent/core`、工具、sandbox、权限系统等核心运行代码写入仍需确认。
- 调整依赖安装识别：`npm install -g`、`pnpm add -g`、`yarn global add`、`go install` 作为宿主全局例外放行；Python/pip 安装必须落到 `agent-alpha/.venv`，禁止 `pip install --user` 和外部 Python 环境。
- 新增 `agent-alpha/skills/install-alpha-skill/SKILL.md`，明确 alpha skill 安装路径、README 路径翻译、依赖命令改写和安装后验证流程。
- 更新系统提示词和相关测试，覆盖 runtime env、sandbox 边界、依赖例外和 `install-alpha-skill` 可加载性。
- 调整 skill 安装推荐路径为 `agent-alpha/home/.agents/skills/<skill-name>`，同时让 `load_skill` 兼容读取 `agent-alpha/skills` 和 `agent-alpha/home/.agents/skills`；同名冲突时保留 `agent-alpha/skills` 优先，兼容 Agent Reach 这类会写入 `~/.agents/skills` 的安装器。
- 修复 skill YAML 头解析：`SkillLoader` 改用 PyYAML 读取 `SKILL.md` frontmatter，支持 `description: >` 和列表字段；系统提示词仍只注入 YAML 摘要，正文继续按需通过 `load_skill` 加载。

## 2026-05-27

### 背景
需要将 `agent-alpha` 从依赖外部 conda 环境的启动方式，调整为项目自带 uv 虚拟环境启动。这样 agent 运行后再调用终端执行 `pip install` 等 Python 依赖命令时，默认会优先落到 `agent-alpha/.venv`，降低污染系统 Python 或 conda 环境的风险。

### 更改内容
- 新增 `agent-alpha/pyproject.toml`，声明 `agent-alpha` 的 Python 包信息、运行依赖和 `agent-alpha` 控制台入口，使 `uv pip install -e .` 可以正常安装项目。
- 新增 `agent-alpha/start-agent-alpha.ps1`，启动时自动检查 uv、创建 `.venv`、设置 `VIRTUAL_ENV` 和 `PATH`，并使用 `.venv` 内的 Python 启动 `agent.cli.main`。
- 调整 uv 启动脚本为 Python 3.12，增加旧 `.venv` 版本检查，并在启动时显式执行 `uv pip install -r requirements.txt`，确保 requirements 里的依赖会被安装到当前 `.venv`。
- 增强启动脚本的失败处理：外部命令返回非 0 时立即停止，并预检 Python 标准库 `tempfile`，避免 uv 管理的 Python 安装损坏时继续进入后续安装或启动流程。

## 2026-05-24

### 背景
`agent-alpha` 的 workspace 模型收束为“一个 agent 实例只绑定一个 workspace”。额外目录不再通过运行时配置输入，后续需要访问其他文件夹时，由用户在对话中直接给出明确路径。

### 更改内容
- 调整 `agent-alpha/agent/core/agent_runtime.py`、`tool_loader.py`、`sandbox_guard.py` 和 `path_policy.py`，删除多 workspace 参数与内部状态，只保留单一 `workspace_root`。
- 调整 `agent-alpha/agent/core/prompt_docs_loader.py` 与 `system_prompt_builder.py`，只从当前 workspace 根目录加载 `AGENTS.md` / `SOUL.md`，系统提示词不再出现 Additional Workspaces。
- 调整 `agent-alpha/agent/core/session_store.py` 和 `agent-alpha/agent/cli/main.py`，session 只记录一个 `workspace`；CLI 删除 `/workspace add`，`/workspace set` 只接受一个路径。
- 清理旧 session 数据，删除 `agent-alpha/session-log/sessions/`、`agent-alpha/session-log/logs/` 和旧版 `agent-alpha/logs/` 下的历史记录，保留目录占位文件。
- 更新相关测试，覆盖单 workspace 入口、提示词、session 保存、沙盒路径判断和 CLI 日志索引行为。

## 2026-04-05

### 背景
Playwright 有头模式此前只在关闭时尝试一次导出 `state/storage/shared.json`。如果浏览器窗口先被手动关闭，导出时机就很脆弱，容易导致 profile 里已经有很多本地状态，但共享登录态文件始终没有落盘。

### 更改内容
- 调整 `agent-alpha/mcp-servers/playwright/playwright.headed.config.json`，将同步触发方式改为 `auto_and_before_close`，并新增 `intervalMs: 2000`。
- 修改 `agent-alpha/mcp-servers/playwright/server.js`，让有头模式运行中定时自动保存 `storage-state`，关闭前再补一次保存，并在保存失败时输出明确错误信息。
- 更新 `agent-alpha/tests/core/test_playwright_mcp_layout.py`，让测试断言与当前有头模式的自动保存行为保持一致。
- 更新 `agent-alpha/mcp-servers/playwright/README.md`，补充有头模式“定时保存 + 关闭前补保存”的说明。

## 2026-04-05

### 背景
需要给 `agent-alpha/mcp-servers/playwright/README.md` 补一段面向人工操作的说明，明确如何手动清理 Playwright 有头模式留下的历史记录和本地 profile。

### 更改内容
- 更新 `agent-alpha/mcp-servers/playwright/README.md`，新增“如何手动清理历史记录”小节。
- 说明手动删除 `state/profiles/default/` 后会清掉历史记录、缓存和本地 profile；如果还要清共享登录态，再额外删除 `state/storage/shared.json`。

## 2026-04-05

### 背景
`agent-alpha/mcp-servers/playwright` 下的浏览器 profile、storage 和临时目录会不断生成运行时数据，需要继续明确这些内容只保留本地，不参与版本管理。

### 更改内容
- 更新 `agent-alpha/.gitignore`，明确忽略 `mcp-servers/playwright/state/profiles/`、`mcp-servers/playwright/temp/` 和 `mcp-servers/playwright/state/storage/` 下的运行时文件。
- 为 `mcp-servers/playwright/state/storage/.gitkeep` 保留白名单规则，避免占位目录被一起忽略。

## 2026-04-05

### 背景
`learning proj/` 主要用于学习和参考，不希望继续参与当前主仓库的版本管理，需要保留本地内容，但从后续提交中排除。

### 更改内容
- 更新根目录 `.gitignore`，新增对 `learning proj/` 的忽略规则。
- 将已被 git 跟踪的 `learning proj/` 从索引中移除，仅保留本地目录和文件，避免后续提交时继续带入学习项目内容。

## 2026-04-05

### 背景
根目录 `调试.txt` 里会放本地调试命令和敏感配置，不适合继续进入版本库，需要让它保留在本地但不再参与后续提交。

### 更改内容
- 更新根目录 `.gitignore`，新增对 `调试.txt` 的忽略规则。
- 将已被 git 跟踪的 `调试.txt` 改为从索引中移除，仅保留本地文件，避免后续提交时再次带上。

## 2026-04-05

### 背景
Playwright MCP 原来在 `state/profiles/` 下长期保留整套有头浏览器 profile，里面会持续积累 `History`、`Cache`、`Sessions` 等运行痕迹。当前目标只需要长期保留登录态，不需要长期保存整套浏览器本地环境。

### 更改内容
- 调整 `agent-alpha/mcp-servers/playwright/playwright.headed.config.json`，将有头模式的 `userDataDir` 从 `state/profiles/default` 改为 `temp/profiles/default`。
- 修改 `agent-alpha/mcp-servers/playwright/server.js`，让有头模式在启动前重建临时 profile 目录，运行过程中定时自动刷新 `state/storage/shared.json`，并在关闭浏览器后自动清理临时 profile。
- 更新 `agent-alpha/mcp-servers/playwright/README.md`，明确 `state/` 只长期保留 `storage/shared.json`，`temp/` 只承载本次有头运行的临时浏览器数据。

## 2026-04-04

### 背景
Playwright 的持久浏览器 profile 目录被纳入了 git 跟踪，里面包含 `Cookies`、`History`、`Cache` 等运行时文件，导致 `git add .` 时既会扫出大量无关改动，也可能因为浏览器锁文件而直接失败。

### 更改内容
- 调整 `agent-alpha/.gitignore`，忽略 `agent-alpha/mcp-servers/playwright/state/profiles/` 整个目录。
- 保留 `agent-alpha/mcp-servers/playwright/state/storage/` 这类轻量登录态目录的思路，不再把整套浏览器运行时 profile 提交进版本库。
- 后续对已被 git 跟踪的 `profiles/` 目录改为从索引中移除，但不删除本地文件，避免影响当前 Playwright 使用。

## 2026-03-21

### 背景
这一轮重点在 `agent-alpha` 的第一阶段重构，目标是先把骨架理顺，不碰第二阶段的多 agent 编排。

### 更改内容
- 将 `Agent` 本体从单一入口里拆出，核心实例放到 `agent-alpha/agent/core/core_agent.py`，让后续单 agent 和多 agent runner 可以分开。
- 新增 `agent-alpha/agent/core/session_paths.py`，统一管理 `workspace_root/sessions/<session_id>/input|output|temp` 和 `workspace_root/logs`。
- 新增 `agent-alpha/agent/core/prompt_docs_loader.py`，让 session 级 `input/AGENTS.md` 和 `input/SOUL.md` 可以自动进入 system prompt。
- 调整 `agent-alpha/agent/core/system_prompt_builder.py`，把 workspace、session、logs、skills、mcp 路径一起注入 prompt。
- 将 skill 加载改成两层模式：启动时只读 `SKILL.md` 的摘要，正文按需加载，不再走“一个 skill 一个 tool”的旧方式。

## 2026-03-21

### 背景
需要把官方 Playwright MCP 接进 `agent-alpha`，同时满足这几个要求：默认有头、浏览器装全局、状态留本地、以后可切无头共享登录态。

### 更改内容
- 在 `agent-alpha/mcp-servers/playwright/` 下建立本地 wrapper，而不是直接依赖运行时 `npx` 临时拉取。
- 增加 `mcp.config.json`、`playwright.headed.config.json`、`playwright.headless.config.json` 和 `server.js`，形成“一个目录、双配置”的结构。
- 约定默认走有头模式，使用持久 profile；无头模式预留为共享 `storage-state` 读取。
- 状态目录固定在 `agent-alpha/mcp-servers/playwright/state/` 下，分为 `profiles/` 和 `storage/`。
- 新增 `agent-alpha/mcp-servers/playwright/README.md`，把“浏览器装全局、状态留本地、关闭前同步 storage-state”的规则写清楚。
- 增加对应测试，确认 Playwright MCP 的目录结构、配置关系和扫描行为正常。

## 2026-03-22

### 背景
原来的 LLM 配置只有一套 `LLM_BASE_URL / LLM_API_KEY / LLM_MODEL_NAME`，已经不适合一个项目里接多家 OpenAI 兼容模型，也不利于后面多 agent 使用不同模型组。

### 更改内容
- 新增 `agent-alpha/config/llm_profiles.json`，将模型配置改为 profile 方式管理。
- 新增 `agent-alpha/agent/core/llm_profiles.py`，负责读取 profile、解析默认项，并从环境变量中取真实 API key。
- 修改 `agent-alpha/agent/core/llm.py`，`LLMClient` 不再直接读取全局常量，而是基于 profile 创建。
- 修改 `agent-alpha/agent/core/core_agent.py`，支持创建 `Agent` 时传入 `llm_profile_name`。
- 保持 `ContextManager` 接口不变，它仍然只接收一个 `llm` 实例，这样以后如果想单独给总结压缩切模型，只需要在 `Agent` 层调整。
- 删除旧的全局 LLM 直连常量，不再保留旧入口，避免两套配置方式并存。

## 2026-03-22

### 背景
这一轮继续整理 `agent-alpha` 的职责边界，目标是让 `Agent` 只负责在一个现成工作目录里运行，把 session 创建、日志落盘这些 runner 级职责从 `Agent` 身上拿掉。

### 更改内容
- 调整 `agent-alpha/agent/core/core_agent.py`，`Agent` 现在只接收外界传入的 `workspace_root`，并自动补齐 `input`、`output`、`temp` 三个目录。
- `Agent` 不再自己生成 `session_id`、不再维护 `logs_dir`、不再负责写 session log；改为提供 `get_session_log_data()`，只暴露日志内容。
- 重写 `agent-alpha/agent/core/session_paths.py`，拆成两类能力：一类给 `Agent` 保证工作目录结构，一类给 CLI/session runner 创建 `sessions/<id>` 和 `logs`。
- 调整 `agent-alpha/agent/core/main.py`，把 session 创建、日志文件名生成、日志写盘、session 索引更新都收回到 `main.py`。
- 重写 `agent-alpha/agent/core/system_prompt_builder.py` 的路径区块，按“当前工作区 / 系统资源区 / 运行记录区”分组，让模型更清楚哪些目录是当前任务目录，哪些是 skill/MCP 资源，哪些是日志记录。

## 2026-03-22

### 背景
继续收束 `Agent` 的 workspace 模型。为了支持单 agent 和以后多 agent 的不同协作方式，这一轮不再把 workspace 固定成 `input/output/temp` 模板，而是改成“一个或多个普通 workspace”，并且只从第一个 workspace 根目录读取人格和规则文档。

### 更改内容
- 修改 `agent-alpha/agent/core/core_agent.py`，`Agent` 现在支持接收一个或多个 workspace；若传多个，只把第一个当作私有/专属 workspace，其他作为额外可工作的 workspace。
- 修改 `agent-alpha/agent/core/prompt_docs_loader.py`，只从第一个 workspace 根目录读取 `AGENTS.md` 和 `SOUL.md`，不再扫描嵌套子目录，也不再从 `input` 目录读取。
- 修改 `agent-alpha/agent/core/system_prompt_builder.py`，路径区块改成“Private Workspace / Additional Workspaces / System Resource Paths / Runtime Records”，并在规则里明确说明：多 workspace 的分工主要由 `AGENTS.md` 解释。
- 修改 `agent-alpha/agent/core/main.py` 和 `agent-alpha/agent/core/session_paths.py`，CLI 会话目录不再自动创建 `input/output/temp`，只创建会话目录本身和日志目录。
- 同步调整测试，确认多 workspace、文档识别范围和日志索引行为符合新模型。

## 2026-03-23

### 背景
开始给 `agent-alpha` 增加真正按路径边界工作的文件类工具沙盒。目标是先把 `read/write/append/edit` 这几类显式文件工具管起来，暂时不碰 bash 路径解析。

### 更改内容
- 新增 `agent-alpha/agent/core/sandbox_types.py`、`agent-alpha/agent/core/path_policy.py`、`agent-alpha/agent/core/sandbox_guard.py`，把“路径分类”“访问决策”“总调度”拆成独立模块，避免继续堆进 `PermissionManager`。
- 路径规则收成三层：workspace 内读写允许；`agent-alpha` 项目目录内但不在 workspace 的读允许、写需要问询；项目外且不在 workspace 的路径直接拒绝。
- `agent-alpha/agent/core/tool_loader.py` 在执行文件类工具前先走 `SandboxGuard`，对拒绝的操作直接返回原因，对需要问询的操作再交给 `PermissionManager`。
- `agent-alpha/agent/core/permission_manager.py` 瘦身为交互层，只保留三种选项：同意一次、拒绝、补充提示词后重试；去掉“本会话一直允许/拒绝”和自动模式切换。
- `agent-alpha/agent/core/core_agent.py` 将当前 agent 的 workspaces 传给 `ToolLoader`，让沙盒能知道哪些目录是当前 agent 允许读写的工作区。
- 新增测试，确认文件类工具在 workspace / 项目目录 / 项目外三类路径上的 allow / ask / deny 行为正确。
## 2026-03-23

### 背景
在文件类工具沙盒已经落地后，继续补齐 bash 的运行时限制。目标不是把 bash 废掉，而是把“可查、可跑脚本、可做简单文件操作”和“危险系统命令、路径不清晰的改文件命令”拆开处理。

### 关键更改
- 新增 `agent-alpha/agent/core/command_path_extractor.py`，把 bash 规则独立出来，先做命令分类，再做路径提取。
- bash 现在分成几类处理：
  - 只读命令：如 `pwd`、`ls`、`cat`、`rg`、`git status/diff/log`
  - 直接运行脚本：`python xxx.py`、`node xxx.js`
  - 包安装命令：如 `pip install`、`npm install`
  - 项目命令：如 `python -m pytest`、`python -m compileall`、`npm run build`
  - 明确改文件命令：如 `mkdir`、`cp`、`mv`、`rm`、`touch`、`echo >`、`tee`、`sed -i`
  - 高危系统命令：如 `sudo`、`format`、`mkfs`、`shutdown`
- `agent-alpha/agent/core/sandbox_guard.py` 现在会单独处理 bash：
  - 只读命令按读权限判断
  - 运行脚本时，脚本文件只要在当前 agent workspace 或 `agent-alpha` 项目目录内就允许
  - 包安装命令和项目命令改成“问询一次”
  - 明确改文件命令会先解析目标路径，再套用已有的 workspace / project / outside 路径规则
  - 解析不出来的改文件命令直接拒绝，并返回“怎样改写才能被解析”的提示
  - 高危系统命令直接拒绝
- `agent-alpha/agent/core/tool_loader.py` 在返回 `Sandbox denied` 或用户拒绝时，会把 bash 的 guidance 一起返回给 agent，方便 agent 自己调整命令写法。
- 新增测试 `agent-alpha/tests/core/test_bash_sandbox.py`，覆盖：
  - python 脚本允许 / 项目外脚本拒绝
  - 包安装问询
  - 项目命令允许
  - 无法解析的改文件 bash 拒绝并返回 guidance
  - 简单可解析的改文件 bash 放行
## 2026-03-23

### 背景
继续微调 bash 沙盒规则。前一版把 `python -m pytest`、`python -m compileall`、`npm/pnpm/yarn run build` 这类项目命令设成了“问询一次”，但这会让常见开发流太卡。

### 关键更改
- `agent-alpha/agent/core/sandbox_guard.py` 里把 `project_command` 从“问询一次”改成“直接允许”。
- 这类命令现在包括常见项目开发命令，例如：
  - `python -m pytest`
  - `python -m compileall`
  - `npm run build`
  - `pnpm run build`
  - `yarn run build`
- 更新 `agent-alpha/tests/core/test_bash_sandbox.py`，把对应断言改成“直接允许且不触发问询”。

## 2026-03-23

### 背景
继续补 bash 沙盒的边缘命令规则，重点处理 git 恢复、git apply，以及包卸载这几类常见但容易误判的命令。

### 关键更改
- `agent-alpha/agent/core/command_path_extractor.py` 增加了对这些命令的识别：
  - `git checkout -- file`
  - `git restore file`
  - `git clean -fd`
  - `pip uninstall`
  - `npm uninstall`
  - `pnpm remove/uninstall`
  - `yarn remove/uninstall`
- `agent-alpha/agent/core/sandbox_guard.py` 增加了 git 边缘规则：
  - `git apply patch.diff`：直接允许
  - `git checkout -- file`、`git restore file`：按“可解析路径改文件”规则处理
  - `git checkout -- .`、`git restore .`：问询一次
  - `git clean -fd`：直接拒绝
- 更新 `agent-alpha/tests/core/test_bash_sandbox.py`，补了这些场景的回归测试，确认边缘命令按新的 allow / ask / deny 规则生效。

## 2026-04-04

### 背景
更新根目录 `AGENTS.md` 里的项目背景说明，让 `learning proj` 的目录名称、项目清单和各项目定位与当前实际内容保持一致。

### 关键更改
- 将背景说明里的 `learnding proj` 更正为实际目录名 `learning proj`。
- 补充 `learning proj` 当前包含的项目：`claw-code` 和 `playwright`，并把项目清单整理成一段更清楚的说明。
- 同步调整 `agent-alpha` 的参考来源描述，使其与 `learning proj` 里当前实际可借鉴的项目保持一致。
# 开发日志

## 2026-04-04

### 背景
为了继续整理 `agent-alpha` 的目录职责，这一轮把“LLM API 适配”和“CLI 包装入口”从 `core` 中拆出来，避免 `core` 同时承担编排层和外部入口层职责。

### 关键更改内容
- 新增 `agent-alpha/agent/api/`，承接 `llm.py` 和 `llm_profiles.py`，明确它们属于外部模型接口适配层。
- 新增 `agent-alpha/agent/cli/`，承接原来 `agent/core/main.py` 的单 agent CLI runner。
- 调整 `agent-alpha/agent/core/core_agent.py` 等内部导入路径，优先使用新的 `api/` 模块。
- 扫描并改掉仓库内旧的 `agent.core.main`、`agent.core.llm`、`agent.core.llm_profiles` 导入后，删除这 3 个兼容文件，避免旧结构继续残留。
- 更新测试和 README 中的关键导入路径，确保目录重构后依然可以运行。
## 2026-04-05

### 背景
agent-alpha 开始按“Agent Runtime 与外层运行框架解耦”的方向重构，先落地最小 runtime 内核、message bus 和 cron 骨架，移除旧的 `core_agent.py` 入口。

### 关键更改内容
- 新增 `agent-alpha/agent/core/agent_runtime.py`，将原 `core_agent.py` 的运行时装配职责迁移到 `AgentRuntime`。
- 新增 `agent-alpha/agent/core/runtime_types.py` 与 `agent-alpha/agent/core/role_config.py`，统一 runtime 请求/响应类型和工具能力策略。
- 删除 `agent-alpha/agent/core/core_agent.py`，CLI、README、测试统一切换到 `agent.core.agent_runtime.AgentRuntime`。
- 新增 `agent-alpha/agent/runtime/bus/` 与 `agent-alpha/agent/runtime/cron/`，提供最小进程内 message bus、cron 触发类型与 service。
- 扩展 `agent-alpha/agent/core/tool_loader.py`，加入按 `RoleConfig` 解析工具集合和 runtime 级工具绑定能力。
- 调整 `agent-alpha/agent/cli/main.py`，改为通过 `RuntimeRequest -> AgentRuntime.handle() -> RuntimeResponse` 调用链运行。
- 新增/调整 runtime、tool policy、bus、cron 相关测试，覆盖新入口、旧入口移除和最小基础设施行为。
## 2026-04-06

### 背景
用 `python -m agent.cli.main` 启动 `agent-alpha` CLI 时，出现了 `agent.cli.main found in sys.modules` 的运行时警告。根因是 `agent/cli/__init__.py` 在包初始化阶段提前导入了 `main.py`，导致同一个模块被包导入和 `-m` 执行各加载一次。

### 关键更改内容
- 修改 `agent-alpha/agent/cli/__init__.py`，移除对 `agent.cli.main` 的提前导入，避免 `python -m agent.cli.main` 时触发重复加载警告。
- 新增 `agent-alpha/tests/core/test_package_layout.py` 中的回归测试，验证导入 `agent.cli` 包时不会顺带预加载 `agent.cli.main`。
## 2026-04-08

### 背景
为了给后续的子 agent、cron 和会话恢复打地基，先重构 `agent-alpha` 的 session 管理。目标是把原来“单次 CLI 临时目录 + 退出时写日志”的模式，调整为“正式 session 快照 + 独立日志目录 + 可恢复对话”。

### 关键更改内容
- 调整 `agent-alpha/agent/core/session_paths.py`，将 CLI 运行目录改为 `agent-alpha/session-log/sessions/` 与 `agent-alpha/session-log/logs/`，默认工作区固定为 `agent-alpha/workspace/`。
- 新增 `agent-alpha/agent/core/session_store.py`，提供 `SessionRecord / SessionKind / SessionStore`，用于保存完整 history 快照、列出最近会话、恢复 interactive 会话，以及记录工作区变更事件。
- 重构 `agent-alpha/agent/cli/main.py`，CLI 现在会在每轮对话后自动保存 session 快照，支持 `/resume` 恢复旧会话，以及 `/workspace` 查看、重设、追加工作区；恢复后改工作区仍归属同一个 session。
- 新增 `agent-alpha/session-log/sessions/.gitkeep` 和 `agent-alpha/session-log/logs/.gitkeep`，显式建立新的 session-log 目录结构。
- 删除旧的 `agent-alpha/sessions/` 目录，不再使用原来的根目录 session 布局。
- 新增和调整 session 相关测试，覆盖 session-log 路径、SessionStore 保存/恢复、工作区变更记录以及 CLI 日志/索引行为。

## 2026-05-29

### 背景
安装 Agent Reach 这类 skill 时，alpha 已能安装 `SKILL.md` 和 Python 包，但 sandbox 不认识 `agent-alpha/.venv` 里的 CLI / Python module 命令，导致 `agent-reach install --env=auto` 被误拒；同时 `auto` 权限模式只是显示状态，没有真正减少 ask 弹窗。

### 关键更改内容
- `agent-alpha/agent/core/command_path_extractor.py` 增加 alpha venv 命令识别：允许 `agent-alpha/.venv` 内的 CLI，以及通过 alpha venv Python 执行的 `python -m <module>` 命令；保留对外部 Python module 命令的拒绝。
- `agent-alpha/agent/core/sandbox_guard.py` 为外部写入拒绝和外部 Python module 拒绝补充合规建议，提示 AI 改用 `agent-alpha/skills`、`agent-alpha/temp` 或 `agent-alpha/.venv`。
- `agent-alpha/agent/core/tool_loader.py` 修正 `auto` 模式：`ask` 级别操作在 auto 下自动放行，但 `deny` 仍然直接拒绝。
- `agent-alpha/tests/core/test_bash_sandbox.py` 增加 Agent Reach 安装命令、alpha venv CLI、auto 放行和 deny 不绕过的回归测试。

## 2026-05-29

### 背景
使用 DeepSeek provider 的 thinking mode 进行工具调用后，后续请求报错：`reasoning_content` 必须回传给 API。根因是 agent loop 在保存 assistant 工具调用消息时只保留了 `content` 和 `tool_calls`，没有保存 DeepSeek 需要的 `reasoning_content`。

### 关键更改内容
- `agent-alpha/agent/core/agent_loop.py` 增加 DeepSeek provider 判断：只要 `provider` 名称包含 `deepseek`（大小写不敏感），且模型返回了 `reasoning_content`，就把它保存到 assistant 工具调用消息中，后续对话会随 history 自动回传。
- `agent-alpha/tests/core/test_agent_loop.py` 增加回归测试，覆盖 DeepSeek provider 会回传 `reasoning_content`、非 DeepSeek provider 不保存该字段两种情况。

## 2026-05-31

### 背景
为了避免在自动化窗口中重新走 Google OAuth 登录，将本机正式 Chrome 的 `Profile 5` 作为 alpha default browser profile 的种子来源。该 profile 对应 `tianyiwang630@gmail.com / 天一`，目标是让 alpha 复用已经登录过的 Chrome profile 数据。

### 关键更改内容
- 新增 `agent-alpha/sync-chrome-profile5-to-alpha.ps1`，脚本通过自身位置自动识别 `agent-alpha` 根目录，不写死项目路径。
- 脚本执行前检查 Chrome / Chromium / agent-browser 进程；如仍在运行则直接中止，不自动关闭进程。
- 脚本将本机 `C:\Users\20157\AppData\Local\Google\Chrome\User Data\Local State` 复制到 alpha default `user-data/Local State`，并将 `Profile 5` 复制到 alpha default `user-data/Default`。
- 脚本会把复制后的 `Local State.profile.info_cache.Default` 映射为 `Profile 5` 的元数据，并将 `last_used` / `profiles_order` 指向 `Default`，避免 alpha 打开 default profile 时显示或识别成其他本机 profile。
- 已执行一次同步，覆盖 `agent-alpha/state/browser/profiles/default/user-data`，该目录属于本地运行状态，不进入 git。

## 2026-05-31

### 背景
用户在新启动的 alpha CLI 中打开有头浏览器后，窗口仍显示 Chromium。最新 session 的工具结果显示 `browser_executable` 仍为 `agent_browser_default`，进程命令行也确认实际启动的是 `C:\Users\20157\.agent-browser\browsers\chrome-149.0.7827.22\chrome.exe`。根因是 browser 后端只读取当前 Python 进程环境变量，未在每次调用时主动读取 `agent-alpha/config/runtime_env.local.json`。

### 关键更改内容
- `agent-alpha/agent/tools/browser_manager.py` 增加对 `config/runtime_env.local.json` 的直接读取：当前进程环境没有 `AGENT_BROWSER_EXECUTABLE_PATH` 时，会从 alpha 本地配置文件取值。
- `_browser_env()` 在路径有效时显式把 `AGENT_BROWSER_EXECUTABLE_PATH` 注入 `agent-browser` 子进程；路径无效时移除该变量并回退默认浏览器。
- `agent-alpha/agent/core/runtime_paths.py` 将 `runtime_env.local.json` 读取改为兼容 UTF-8 BOM，并新增 `apply_runtime_env()`，作为 alpha 统一运行环境注入口。
- `agent-alpha/agent/cli/main.py` 和 `agent-alpha/agent/core/agent_runtime.py` 启动时调用 `apply_runtime_env()`，确保路径、token、cookie、proxy 等持久变量进入 alpha Python 进程环境，而不只是 bash 工具环境。
- `agent-alpha/tests/core/test_browser_tools.py` 和 `agent-alpha/tests/core/test_runtime_paths.py` 新增回归测试，覆盖“进程环境没有变量，但 runtime_env.local.json 中配置了正式 Chrome 路径”和“UTF-8 BOM 配置文件仍能加载”的场景。

## 2026-05-30

### 背景
`agent-browser install` 下载的 Chrome for Testing 在 Google 等登录敏感网站上容易被识别为测试/自动化浏览器；同时第一版 browser profile 主要依赖 `state.json` 保存 cookies/localStorage，不符合已对齐的“长期完整 Chrome profile + 无头临时副本”设计。

### 关键更改内容
- `agent-alpha/config/runtime_env.local.json` 新增长期环境变量 `AGENT_BROWSER_EXECUTABLE_PATH`，优先让 `agent-browser` 使用本机正式 Chrome；如果该路径失效，browser 后端会回退到 agent-browser 默认浏览器，并在工具结果中明确提示。
- `agent-alpha/agent/tools/browser_manager.py` 将 browser profile 主体升级为完整 `user-data` 目录：长期 profile 位于 `state/browser/profiles/<name>/user-data`，`state.json` 仅保留兼容。
- 有头登录模式直接使用长期 `user-data`，登录态由 Chrome profile 自然持久化，关闭时不再调用 `agent-browser state save`。
- 无头模式启动时复制长期 profile 到 `state/browser/runtime/<session_id>/user-data`，任务结束关闭 session 后清理该 runtime 临时目录，默认不回写长期 profile。
- 增加简单 profile 锁：有头模式占用长期 profile 时，无头复制同一 profile 会返回清楚错误，避免复制到一半的登录态。
- 更新 `agent-alpha/agent/tools/browser_tool.py` 的工具描述，让大模型只感知 profile 名称和有头/无头语义，不需要自己处理真实路径。
- 更新 `agent-alpha/tests/core/test_browser_tools.py`，覆盖正式 Chrome 路由提示、完整 profile 创建、有头不再 state save、无头临时副本和关闭清理。

## 2026-06-06

### 背景
分析 `agent-alpha/session-log/events/d22ff2.jsonl` 时确认，`agent-reach` skill 已成功加载，但当前安装副本仍提示使用不存在的 `bird` 命令；同时 `pip install --user` 类工具可能落到 `agent-alpha/home/.local/Python*/Scripts`，此前 bash 运行环境不会自动找到这些 CLI。

### 关键更改内容
- 更新 `agent-alpha/home/.agents/skills/agent-reach/SKILL.md` 的 Twitter/X 说明，将主命令改为当前已安装可用的 `twitter status`、`twitter search`、`twitter tweet`、`twitter user-posts`、`twitter feed`。
- `agent-alpha/agent/core/runtime_paths.py` 在生成 bash 运行环境时，把已存在的 `agent-alpha/home/.local/Python*/Scripts` 追加到 PATH 前缀，作为通用 CLI skill 运行底座的一部分。
- 补充 `agent-alpha/tests/core/test_runtime_paths.py` 回归测试，并更新 `agent-alpha/路径管理说明.md` 的 PATH 顺序说明。

## 2026-05-29

### 背景
在 Reddit / Agent Reach 相关会话里，sandbox 把带 URL 和 `2>&1` 的 bash / PowerShell 命令误判成“向 alpha 外部写文件”，导致本来只是联网读取的命令也被拦截；同时 `cp`、`Copy-Item` 这类“从外部读取、写入 alpha 内部”的命令也会被错拦。

### 关键更改内容
- `agent-alpha/agent/core/command_path_extractor.py` 收窄了通用写路径识别：不再扫描整条命令里所有“像路径的字符串”，而是只识别真正的写目标，例如重定向输出文件、`-o/--output`、PowerShell 的 `Set-Content` / `Add-Content` / `Out-File` / `New-Item` / `Remove-Item`，以及 `Copy-Item` / `Move-Item`、cmd 的 `copy` / `move`。
- 同文件补充了 URL 排除规则，避免把 `https://.../search.json` 这种网络地址里的路径段误当成本地文件路径。
- `cp` 的路径判定改为只检查目标路径，允许“从 alpha 外部读取、写入 alpha 内部”，同时继续禁止写入 alpha 外部；`mv` 仍按会修改源和目标两端处理。
- `agent-alpha/agent/core/sandbox_guard.py` 在识别出“只写入允许目录”的通用命令后，明确返回 `allow + write`，不再落回 `unknown`。
- `agent-alpha/tests/core/test_bash_sandbox.py` 新增回归测试，覆盖 bash URL + `2>&1`、PowerShell URL + `2>&1`、`cp` 外部读入内部、`Copy-Item` 外部读入内部等场景。

## 2026-06-09

### 背景
排查 `agent-alpha/session-log/events/9b183b.jsonl` 时发现，上下文压缩后会把最近历史切在 `tool` 消息开头，导致该 `tool` 结果失去前置的 `assistant tool_calls`，OpenAI 接口返回 `Messages with role 'tool' must be a response to a preceding message with 'tool_calls'`。

### 关键更改内容
- `agent-alpha` 新增消息历史合法化逻辑，压缩和发送 LLM 前都会保证 `assistant tool_calls` 与对应 `tool` 结果不被切散，并丢弃孤立 `tool` 结果。
- `ContextManager` 压缩保留最近上下文时改为按完整消息组收集；压缩节点输入改为纯文本对话转写，不再直接传原始 messages JSON。
- 压缩失败时不再机械截断固定条数，而是退回到不超过压缩触发阈值 30% 的最近完整消息组，并提示用户压缩失败。
- 工具输出截断提示改为明确说明原始长度、保留长度和截断位置；读文件/文档类工具允许更高输出上限。
- 补充 `test_context_manager.py`、`test_agent_loop.py`、`test_session_events.py` 回归测试，覆盖压缩边界、孤立 tool 防线、fallback 和截断提示。

## 2026-05-29

### 背景
最新 session 里，`agent-reach` 实际可用的 Twitter/X CLI 是 `twitter`，但安装在 `agent-alpha/home/.agents/skills/agent-reach/SKILL.md` 的主说明仍写着旧的 `bird` / `user-tweets` 用法，导致 agent 每次先执行不存在的 `bird` 命令，再绕路排查。

### 关键更改内容
- 更新 `agent-alpha/home/.agents/skills/agent-reach/SKILL.md` 的 Twitter/X 段，将主入口改为 `twitter search`、`twitter tweet`、`twitter user-posts`、`twitter thread`。
- 明确提示 agent-alpha 中应直接调用 `twitter`，不要把 `bird` 或 `twitter-cli` 当作可执行命令名。
- 调整 Twitter troubleshooting，提示时间线优先使用 `twitter user-posts` / `twitter feed`，`twitter search` 可能受 X 限流影响。

## 2026-05-29

### 背景
安装 Agent Reach 之后，Python CLI 同时出现在 `agent-alpha/.venv` 和 `agent-alpha/home/pipx/venvs`，导致后续排查 PATH 和实际执行入口时容易混乱。新的约定是：alpha 自己和 skill 直接调用的 Python CLI 优先安装到 `agent-alpha/.venv`，sandbox 不因为使用 pipx / uv tool 这类安装风格而拒绝。

### 关键更改内容
- 更新 `agent-alpha/skills/install-alpha-skill/SKILL.md`，明确 Python 依赖和 Python CLI 工具都优先安装到 `agent-alpha/.venv`。
- 将 README 里的 `pipx install ...` / `uv tool install ...` 说明为需要优先翻译成 `.venv` 安装方式，避免默认装出第二套 Python CLI 环境。
- 更新 `agent-alpha/agent/core/system_prompt_builder.py` 的系统提示词，告诉 agent 遇到第三方 Python CLI 安装说明时优先使用 `.venv/Scripts/python.exe -m pip install ...` 或 `.venv/bin/python -m pip install ...`。
- 保持 PATH 策略不变：`agent-alpha/.venv/Scripts` 或 `agent-alpha/.venv/bin` 已经在 PATH 最前面，确保 `.venv` 里的 CLI 优先被调用。

## 2026-05-29

### 背景
为了后续继续开发 browser 工具和其它 CLI / skill 功能，需要把 `agent-alpha` 当前的路径管理状态整理成一份可直接查阅的项目文档，避免每次都从代码里重新梳理重定向变量、目录布局和环境边界。

### 关键更改内容
- 新增 `agent-alpha/路径管理说明.md`，梳理当前启动流程、环境变量重定向、PATH 顺序、skill 双路径、Python `.venv` 策略、npm / Go 全局语义以及 sandbox 路径边界。
- 记录 browser 相关预留目录 `agent-alpha/state/browser/*` 已经在运行目录初始化中创建，可作为后续 browser 工具的状态落点。

## 2026-05-29

### 背景
browser 这类工具可能通过 npm 或 Go 全局安装，实际可执行文件会位于 `C:\Users\<user>\AppData\Roaming\npm`、`C:\Users\<user>\go\bin` 等 alpha 外部路径。sandbox 需要允许执行这些外部 CLI / exe，同时继续禁止 agent 直接写入 alpha 外部文件。

### 关键更改内容
- `agent-alpha/agent/core/command_path_extractor.py` 新增外部可执行文件调用识别，显式区分“执行外部 CLI / exe”和“写外部文件”。
- `agent-alpha/agent/core/sandbox_guard.py` 在外部写路径检查之后放行纯外部 CLI / exe 执行，避免误拦 npm / Go 全局工具。
- `agent-alpha/agent/core/system_prompt_builder.py` 补充 bash 规则：允许执行 host path 上的已安装 CLI / exe，但禁止通过参数、重定向、复制、移动、删除等方式写出 `agent-alpha` 和当前 workspace。
- `agent-alpha/tests/core/test_bash_sandbox.py` 增加回归测试，覆盖外部 Go exe、npm cmd、PowerShell / cmd 调用，以及 `--output` / 重定向写外部路径的拒绝场景。
- 更新 `agent-alpha/路径管理说明.md`，记录外部 CLI / exe 的执行边界。

## 2026-05-29

### 背景
为 `agent-alpha` 补第一版浏览器自动化能力，目标覆盖本地无头执行、用户有头登录保存登录态、外部 CDP 接管，以及后续 subagent / 多 agent 只开放无头工具的权限控制。根据 `agent-browser` 当前能力，第一版用 `--session`、`--headed`、`--cdp` 和 `state save/load` 管理浏览器会话与登录态。

### 关键更改内容
- 新增 `agent-alpha/agent/tools/browser_manager.py`，集中管理 `state/browser` 下的 profile registry、浏览器 session、下载目录、socket 目录，并封装 `agent-browser` CLI 调用。
- 新增 `agent-alpha/agent/tools/browser_tool.py`，提供 `browser_navigate`、`browser_snapshot`、`browser_click`、`browser_type`、`browser_scroll`、`browser_press`、`browser_close`、`profile_list`、`profile_create`、`profile_login_headed`、`browser_connect_cdp`、`browser_disconnect_cdp`、`browser_cdp_status`。
- 更新 `agent-alpha/agent/core/tool_loader.py`，把浏览器工具接入内置工具加载，并按 `browser_headless`、`browser_profile`、`browser_cdp` 三组注册权限，便于 main agent、subagent、多 agent 使用不同工具面。
- 更新 `agent-alpha/agent/core/runtime_paths.py`，初始化 `state/browser/profiles`、`state/browser/sessions`、`state/browser/sockets`、`state/browser/downloads` 目录。
- 更新 `agent-alpha/.gitignore`，保留运行时 `tools/` 目录忽略规则，同时明确放行 `agent-alpha/agent/tools/` 下的源码文件，避免新增内置工具被误忽略。
- 新增 `agent-alpha/tests/core/test_browser_tools.py`，覆盖默认 profile 创建、headless 加载 profile state、有头登录关闭时保存 state，以及浏览器工具组权限过滤。

## 2026-05-29

### 背景
交互式 CLI 中双击 ESC 后，只设置了中断标记；如果当前回合正在等待模型请求、bash 长命令或 `agent-browser` 有头进程，主循环仍可能继续阻塞，导致终端无法立刻回到 `You:` 输入状态。

### 关键更改内容
- `agent-alpha/agent/core/tool_loader.py` 增加共享 interrupt event 注入能力，并把同一个 ESC 中断信号传给支持中断的内置工具。
- `agent-alpha/agent/core/agent_runtime.py` 在工具加载前绑定 runtime 的 `_interrupted` event，确保每个回合的 ESC 信号能传到工具层。
- `agent-alpha/agent/core/agent_loop.py` 将模型等待线程改为 daemon，ESC 后立即放弃等待并返回 `[用户中断] 已停止当前任务。`，工具返回中断后也不再继续后续模型轮次。
- `agent-alpha/agent/tools/bash_tool.py` 使用 `subprocess.Popen()` 轮询执行，ESC 后通过进程树终止当前 shell 和子进程，并返回 `interrupted: true`。
- `agent-alpha/agent/tools/browser_manager.py` 对 `agent-browser` CLI 调用增加同样的中断轮询和进程树终止，避免有头浏览器命令阻塞 CLI。
- 新增 `agent-alpha/agent/tools/process_utils.py`，集中封装 Windows `taskkill /T /F` 与 POSIX process group 终止逻辑。
- 补充 `agent-alpha/tests/core/test_agent_loop.py`、`test_bash_interrupt.py`、`test_browser_tools.py`，覆盖模型等待中断、bash 长进程中断、browser CLI 中断和 ToolLoader 注入。

## 2026-06-08

### 背景
排查 `agent-alpha/session-log/events/fb18ba.jsonl` 时发现，双击 ESC 对 bash / browser 等内置工具已有中断能力，但 MCP / websearch 这类工具如果自身阻塞，主循环仍会同步等待当前工具返回，只能跳过后续工具调用，无法快速回到交互输入。

### 关键更改内容
- `agent-alpha/agent/core/agent_loop.py` 将单个工具调用的等待过程改为可中断等待：工具仍按原方式执行，但主循环会轮询共享 interrupt event，收到 ESC 后立即记录该工具为“用户中断”并停止当前回合。
- 保留 bash / browser 工具内部已有的进程树终止逻辑；本次改动补齐 MCP / 其它不可直接杀进程工具的主循环响应能力，后续如增加前端停止按钮或其它中断来源，可以继续复用同一个 interrupt event。
- `agent-alpha/tests/core/test_agent_loop.py` 新增阻塞工具等待中断回归测试，覆盖“工具调用自身迟迟不返回，但 ESC 信号到达后主循环应快速返回”的场景。

## 2026-06-09

### 背景
调整 `agent-alpha` 的上下文窗口上限配置。

### 关键更改内容
- `agent-alpha/agent/core/config.py` 将 `MAX_CONTEXT_TOKENS` 从 `200000` 提升到 `400000`，其余压缩与输出相关参数保持不变。

## 2026-06-09

### 背景
`agent-alpha` 浏览器工具在 Windows 上通过 npm 生成的 `agent-browser.cmd` 启动时，URL 中的 `&order=...` 可能被 `cmd.exe` 当作命令分隔符解释；同时外部进程 stderr 如果输出 GBK 中文报错，会触发 `UnicodeDecodeError`，导致浏览器主结果被二次异常覆盖。

### 关键更改内容
- `agent-alpha/agent/tools/browser_manager.py` 在 Windows 上检测到 `agent-browser.cmd` / `.bat` / `.ps1` shim 时，优先解析并直连同级 `node_modules/agent-browser/bin/agent-browser-win32-x64.exe`，避免 URL 参数进入 shell 包装层。
- 增加外部进程 stdout/stderr 容错读取逻辑，按 UTF-8、系统默认编码、GBK 逐级尝试，最后使用替换字符兜底，避免诊断输出编码问题打穿工具主流程。
- `agent-alpha/tests/core/test_browser_tools.py` 增加回归测试，覆盖 Windows npm shim 解析为原生 exe，以及 GBK stderr 不再触发 `UnicodeDecodeError`。

## 2026-06-10

### 背景
`agent-alpha` 在长会话触发上下文压缩时，要求模型输出 JSON 摘要；当模型返回包含中文、引号、花括号或不完整结构的摘要文本时，`json.loads()` 会失败，导致压缩回退到最近短上下文，丢失较多任务历史。

### 关键更改内容
- `agent-alpha/agent/core/context_manager.py` 将压缩摘要改为纯 Markdown/文本，不再要求模型输出 JSON，也不再解析 JSON 或转换 Markdown。
- 压缩后的摘要直接作为一条 `user` 消息进入新的 history，供后续 agent loop 继续使用。
- `agent-alpha/agent/core/config.py` 将 `LLM_SUMMARY_MAX_TOKENS` 从 `4000` 提升到 `6000`，给长任务摘要更多输出空间。
- `agent-alpha/tests/core/test_context_manager.py` 增加回归测试，覆盖“摘要文本包含中文、引号和坏 JSON 片段时仍能正常压缩”的场景。

## 2026-06-11

### 背景
`agent-alpha` 日常启动脚本每次都会重新执行依赖安装和 editable 安装，启动时可能卡在 `uv` 下载/解析依赖阶段，例如停在 `certifi==2026.5.20`，导致还没进入 agent CLI 就阻塞。

### 关键更改内容
- 新增 `agent-alpha/setup-agent-alpha.ps1`，专门负责首次或依赖变更后的环境准备：创建 `.venv`、检查 Python 3.12、加载 `runtime_paths.py` 生成的运行时环境变量、安装 `requirements.txt`、安装 editable 包。
- 精简 `agent-alpha/start-agent-alpha.ps1`，日常启动只检查 `.venv` 是否存在、校验 Python 和运行时目录，然后直接启动 `python -m agent.cli.main`。
- 启动脚本在缺少 `.venv` 时提示先运行 `.\setup-agent-alpha.ps1`，避免静默进入安装流程。
- 更新 `agent-alpha/路径管理说明.md`，区分“首次起环境/依赖变更”和“日常启动”的职责边界。

## 2026-06-12

### 背景
安装第三方 skill 时，agent 已经在 `agent-alpha` 目录内运行 bash，但仍按 `agent-alpha/temp/...`、`agent-alpha/home/...` 拼接相对路径，导致真实落点变成错误的 `agent-alpha/agent-alpha/...` 嵌套目录。

### 关键更改内容
- 更新 `agent-alpha/skills/install-alpha-skill/SKILL.md`，明确 bash 当前 cwd 已经是 `AGENT_ALPHA_ROOT`，命令示例必须使用 `temp/...`、`home/.agents/skills/...`、`skills/...`，只在概念说明里使用 `agent-alpha/...`。
- 更新 `agent-alpha/agent/core/system_prompt_builder.py`，在系统提示词的 Bash Runtime 中说明不要在 bash command 里写 `agent-alpha/home`、`agent-alpha/temp`、`agent-alpha/skills` 等 project-root 前缀路径。
- 更新 `agent-alpha/agent/tools/bash_tool.py`，在命令执行前硬拦截 `agent-alpha/home`、`agent-alpha/temp`、`agent-alpha/skills`、`agent-alpha/cache`、`agent-alpha/config`、`agent-alpha/state`、`agent-alpha/workspace` 这类相对路径错位写法，并返回合规改写建议。
- 补充 `agent-alpha/tests/core/test_bash_interrupt.py`、`test_system_prompt_builder.py`、`test_install_alpha_skill.py`，覆盖错位路径拒绝、绝对路径不误判、系统提示词和安装 skill 文档规则。

## 2026-06-12

### 背景
长对话需要手动触发上下文压缩，并且无论手动还是自动触发，压缩结果都应在终端、实时事件、会话快照和最终归档日志中可见；手动压缩失败时不能 silently fallback 或改写当前历史。

### 关键更改内容
- 新增 `/compact` CLI 命令，手动强制压缩当前对话历史；成功时展示压缩前后消息数、token 数和压缩摘要正文。
- `agent-alpha/agent/core/context_manager.py` 增加 `ContextCompressionResult` 和带结果信息的压缩入口，区分手动压缩失败不改 history、自动压缩失败 fallback 到最近短上下文。
- `agent-alpha/agent/core/agent_runtime.py` 记录自动压缩事件到 `runtime_events`，并在自动压缩成功或 fallback 时输出终端提示，最终随 `logs/*.json` 归档。
- `agent-alpha/agent/core/session_events.py` 支持写入 `context_compacted` / `context_compaction_failed` 这类非 history 实时事件，保留摘要、触发方式、token 变化和错误信息。
- `agent-alpha/agent/core/session_store.py` 增加会话事件追加能力，`sessions/*.json` 会记录压缩成功或失败事件。
- 补充 `agent-alpha/tests/core/test_context_manager.py`、`test_session_events.py`、`test_session_store.py`、`test_main.py`、`test_core_agent.py`，覆盖手动成功、手动失败不改历史、空历史、自动成功、自动 fallback、摘要特殊字符保留和日志顺序。

## 2026-06-13

### 背景
`agent-alpha` 的 Bash 工具在 Windows 上容易被宿主 Python、Conda 环境、Microsoft Store Python alias 或本机编码设置影响，导致同一条命令在不同机器上表现不一致；同时 sandbox 拒绝外部写路径时，提示缺少足够的路径诊断，用户不容易知道该改成哪个可写位置。

### 关键更改内容
- `agent-alpha/agent/core/runtime_paths.py` 固定 Bash 运行时优先使用 `agent-alpha/.venv`：Windows 将 `.venv/Scripts` 放到 PATH 首位，macOS/Linux 将 `.venv/bin` 放到 PATH 首位；清理宿主 Python/Conda 污染变量，并注入 `PYTHONNOUSERSITE=1`。
- Windows 默认注入 `PYTHONUTF8=1` 和 `PYTHONIOENCODING=utf-8`，但允许 `runtime_env.local.json` 显式覆盖编码变量；PATH、HOME、VIRTUAL_ENV 等运行边界变量仍不可被配置文件覆盖，并按大小写不敏感方式保护。
- `agent-alpha/agent/core/command_path_extractor.py` 和 `agent-alpha/agent/core/sandbox_guard.py` 增加可静态识别的 Python launcher 检查：拒绝 `py`、外部绝对路径 Python、`conda run ... python`、`uv run --python <external>` 以及命令内联 PATH 覆盖后调用 Python，不自动改写用户命令。
- `agent-alpha/agent/tools/bash_tool.py` 在执行 Python 命令前确认 `.venv` 内 Python 存在且可解析；`.venv` 缺失或解析不到时明确失败，不回退到宿主 Python；遇到 Microsoft Store Python alias 文案时保留原 stdout/stderr 并追加使用 alpha `.venv` 的提示。
- `agent-alpha/agent/core/sandbox_guard.py` 增强外部写路径拒绝提示，补充原始路径、解析后路径、当前 cwd、允许写入根目录和推荐写法；提示只用于诊断，不改变 allow/ask/deny 决策。
- 补充 `agent-alpha/tests/core/test_runtime_paths.py`、`test_bash_sandbox.py`、`test_bash_tool_working_dir.py`、`test_bash_output_streaming.py`，覆盖 Windows/macOS PATH、编码覆盖、Conda 污染清理、大小写 PATH 绕过、`.venv` 缺失、Python launcher 绕过、Microsoft Store alias 和流式输出边界。
- 进一步收紧 Python 路径判定：`.venv/.../python` 这类相对路径按 `agent-alpha` 根目录解析，支持 `..` 规范化；`uv run --python` 只允许 `.venv` 内真实 Python 名称，避免把 `.venv` 内任意 exe 当作解释器放行。
- 补充 macOS 风格绝对路径、多 agent 不同 `project_root`、同一 agent 多 BashTool 实例的隔离测试；修复同进程多 agent 轮流调用 `apply_runtime_env` 时，上一个 agent 的 `runtime_env.local.json` 自定义变量残留到下一个 agent 的问题。
- 固定 `open-websearch` MCP 版本为 `open-websearch@2.1.11`，并按当前 Clash TUN fake-IP 配置注入 `FAKE_IP_CIDRS=198.18.0.0/16`；同步重新生成 `mcp-servers/open-websearch/auto-config.json`，减少 fake-IP DNS 结果被误判为私网目标。

## 2026-06-14

### 背景
`agent-alpha` 浏览器工具在打开网页时会把页面快照随 `browser_navigate` 一起写入 history，复杂网页会让后续“继续”请求携带大量网页文本，存在触发上游 `Content Exists Risk` 的风险。

### 关键更改内容
- `agent-alpha/agent/tools/browser_manager.py` 调整 `browser_navigate` 链路，打开网页后只返回导航摘要，不再自动调用 `snapshot -i`。
- `agent-alpha/agent/tools/browser_manager.py` 新增浏览器快照瘦身逻辑：默认 `browser_snapshot` 只保留 heading、link、button、输入框等带 ref 的可行动元素；完整快照通过 `save_full=true` 写入 `temp/browser_snapshots`，history 只保留文件路径和统计信息。
- `agent-alpha/agent/tools/browser_tool.py` 更新工具描述，新增推荐参数 `save_full`，并保留 `full` 作为兼容别名。
- `agent-alpha/agent/core/session_events.py` 增加浏览器工具专用输出上限，`browser_navigate` 收紧到约 5000 字符，`browser_snapshot` 收紧到约 10000 字符，同时保留文档读取工具的 50000 字符放宽规则。
- `agent-alpha/agent/cli/main.py` 在通用异常分支写入 `runtime_error` 实时事件，但不覆盖可恢复 session 快照，避免把触发错误的脏 history 固化。
- 补充 `agent-alpha/tests/core/test_browser_tools.py`、`test_session_events.py`、`test_main.py`，覆盖导航不带快照、快照瘦身、完整快照写文件、浏览器专用截断、异常事件不污染恢复快照等边界场景。
